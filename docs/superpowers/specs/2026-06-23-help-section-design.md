# Дизайн: Справка (описание игры и механик)

**Дата:** 2026-06-23  
**Статус:** утверждено (brainstorming)  
**Связь:** `docs/superpowers/specs/2026-03-28-gen-game-design.md`, `docs/superpowers/specs/2026-06-18-codex-design.md`, `docs/superpowers/specs/2026-06-22-party-squad-expedition-design.md`, `docs/superpowers/specs/2026-06-23-memento-modifiers-design.md`, `memento-wiki.md`, `src/features/campaign/CampaignHub.tsx`, `src/features/campaign/CampaignHubNav.tsx`

## 1. Цель

Вкладка **«Справка»** в хабе кампании — компактный справочник **общих правил** Gen: что за игра, как устроен бой, прогресс, отряд, экономика. Отдельно от **Кодекса** (энциклопедия конкретных сущностей, открываемых по ходу игры).

**Обязательное требование:** точное (по коду и спекам) описание системы **Memento Mori** в **игровом языке** — без формул и таблиц шансов; допустимы качественные формулировки и примеры («на уровне 1 почти всегда»).

## 2. Принятые решения (brainstorming)

| Вопрос | Решение |
|--------|---------|
| Место в UI | 6-я вкладка хаба: Персонаж · Бой · Магазин · Таверна · Кодекс · **Справка** |
| Доступность | **Всегда** — вкладка **не disabled** в бою, expedition и inter_battle (`CampaignBattleNav` переиспользует `CampaignHubNav`) |
| Глубина Memento Mori | Игровой язык (A): три оси, триггеры, без формул; числа только в примерах |
| Архитектура контента | Статический каталог статей в `src/game/help/` (подход A) |
| Навигация внутри | `Collapse` Ant Design; первый раздел **Memento Mori** раскрыт по умолчанию |
| Поиск | Не в MVP — статей мало, один скролл |
| Персистентность | Не нужна — контент одинаков для всех сохранений |
| Связь с Кодексом | В тексте Справки — пояснение различия; ссылки «подробнее в Кодексе» где уместно (моды, враги) |

## 3. Отличие от Кодекса

| | **Справка** | **Кодекс** |
|---|-------------|------------|
| Содержание | Правила и механики | Конкретные предметы, умения, моды, враги |
| Открытие | Всё сразу | Прогрессивно по триггерам |
| Доступ в бою | Да | Нет (disabled при `inBattle`) |
| Источник данных | `src/game/help/articles.ts` | `src/game/codex/registry.ts` + каталоги контента |

## 4. Макет

```
┌─────────────────────────────────────────────┐
│  HUD (золото, worldPower, …)                │
├─────────────────────────────────────────────┤
│  [Персонаж][Бой][Магазин][Таверна][Кодекс][Справка] │
├─────────────────────────────────────────────┤
│  ▼ Memento Mori          ← defaultActive   │
│  ▼ Бой                                      │
│  ▼ Карты и умения                           │
│  ▼ Экипировка и модификаторы                │
│  ▼ Отряд и expedition                     │
│  ▼ Таверна и магазин                        │
│  ▼ Кодекс                                   │
│  ▼ О Gen                                    │
└─────────────────────────────────────────────┘
```

- Иконка вкладки: `QuestionCircleOutlined` (Ant Design Icons).
- Размер списка: `size="small"` у `Collapse`, как в AGENTS.md для плотных экранов.
- Сообщения/toast не используются — только статический текст.

## 5. Архитектура реализации (рекомендованный подход A)

| Путь | Ответственность |
|------|-----------------|
| `src/game/help/articles.ts` | `HelpArticle[]`: `id`, `title`, `paragraphs: string[]` (и опционально `bullets`) |
| `src/game/help/articles.test.ts` | Снапшот id/заголовков; smoke на непустые параграфы |
| `src/features/help/CampaignHelpTab.tsx` | Рендер `Collapse` из каталога |
| `src/features/campaign/campaignHubShared.ts` | `CampaignHubTab` += `'help'` |
| `src/features/campaign/CampaignHubNav.tsx` | Кнопка Справка; **без** `helpDisabled` |
| `src/features/campaign/CampaignHub.tsx` | Рендер `CampaignHelpTab` при `activeTab === 'help'` |
| `src/features/campaign/CampaignBattleNav.tsx` | Без изменений логики disabled (help не в списке блокируемых) |

**Не делать в MVP:** Drawer из HUD, MDX/markdown-пайплайн, отдельный роут, дублирование формул из `rollCardLevelUp` / `resolvePercentToken` в UI.

## 6. Каталог статей (контент)

Порядок фиксирован. Тексты — нормативные для UI (редактирование только здесь и в `articles.ts`).

### 6.1. `memento` — Memento Mori

**Gen** построен на системе **Memento Mori** — прогресс не обнуляется с поражением. Сила накапливается в трёх направлениях:

| Когда | Что растёт |
|-------|------------|
| **Смерть** | Уровень персонажа; сила мира — враги в следующих боях становятся опаснее |
| **Использование** | Уровень умений и надетой экипировки |
| **Победа** | Уровень модификаторов в слотах карт и предметов |

**Смерть.** Когда падает союзник — у него есть шанс получить +1 к уровню персонажа (сохраняется между боями). Когда умирает враг — растёт **сила мира**: следующие противники выходят сильнее, даже если это другой тип врага.

**Использование.** Каждое применение умения в бою — шанс повысить его уровень. На **уровне 1** почти всегда срабатывает; дальше шанс падает, на очень высоких уровнях рост редкий, но возможен. Числа в описании умения (урон, лечение) **растут вместе с уровнем**.

- **Оружие** качается, когда вы атакуете им.
- **Броня и аксессуары** — когда по вам попадают.
- Без оружия («кулаки») предмет не качается.

**Победа.** После победы в бою каждый **установленный модификатор** на умении или предмете получает свой шанс +1 к уровню модификатора — независимо от уровня самой карты.

**Модификаторы** открываются на **вехах уровня** носителя (умения или предмета): первый слот — с высокого уровня, следующие — ещё выше. При открытии слота выбираете **1 из 3** предложенных модов. Уровень мода и уровень карты **растут отдельно**: мод не мешает прокачке умения за использование.

*Соответствие коду:* `rollCardLevelUp`, `worldPower`, `unitLevel`, `itemProgress`, рост `Lm` за победу, вехи `modSlotMilestones` (prod: 75, 175, 275…). В UI **не** указывать dev-вехи (5, 10, 15).

### 6.2. `battle` — Бой

- Пошаговая тактика на **квадратной сетке**, движение в **4 стороны**.
- Каждый раунд порядок хода по **инициативе** (⚡): класс, экипировка и умения влияют на неё.
- У вас **2 слота умений** в бою (loadout) — остальная коллекция в хабе.
- Ближний и **дальний** бой; у дальних атак есть **дальность** — смотрите подсветку клеток.
- HP = 0 → персонаж **падает** (downed), но не исчезает — его можно поднять умением, предметом или между боями.
- **Поражение** не стирает кампанию: можно повторить бой с сохранённым прогрессом.

### 6.3. `cards` — Карты и умения

- У каждого персонажа своя **коллекция карт** и свой **loadout** (2 активных слота).
- Умение имеет **уровень** — растёт при использовании в бою (см. Memento Mori).
- Описания с растущими числами отражают текущий уровень.
- Карта **Удар** — базовая атака; качает **оружие**, а не себя.

### 6.4. `equipment` — Экипировка и модификаторы

- Три слота: **оружие**, **броня**, **аксессуар**.
- Предметы лежат в **инвентаре** персонажа; между бойцами отряда **не передаются автоматически** — только вручную в хабе.
- У надетых предметов свой **уровень** и **слоты модификаторов** — по тем же правилам, что у умений.
- Детали конкретных модов — в **Кодексе** (категория «Модификаторы»).

### 6.5. `squad` — Отряд и expedition

- В **отряде** до 4 бойцов (состав меняется в хабе).
- **Expedition** — цепочка боёв без возврата в хаб: магазин, таверна, смена состава и экипировки **заморожены**.
- Упавший в expedition боец **не участвует** в следующем, пока не воскресят между боями — **заменить из резерва нельзя**.
- После завершения expedition — полный доступ к хабу; упавшие снова доступны.

### 6.6. `economy` — Таверна и магазин

- **Таверна** — найм новых персонажей за золото (класс, статы, стартовая экипировка).
- **Магазин** — покупка предметов; ассортимент в хабе.
- Оба недоступны во время expedition и боя.

### 6.7. `codex` — Кодекс

- **Кодекс** — энциклопедия **конкретных** предметов, умений, модов и врагов, которых вы уже встретили.
- Открывается по ходу игры: купили предмет, применили умение, убили врага, прокачали мод.
- **Справка** объясняет **общие правила**; Кодекс — детали по каждой сущности.

### 6.8. `about` — О Gen

**Gen** — тактическая RPG: собираете отряд, ведёте expedition по цепочке сценариев, качаете умения и экипировку через Memento Mori. Прогресс сохраняется автоматически.

## 7. Вне объёма MVP

- Таблицы шансов, формулы `%%`, интерактивные калькуляторы
- Классы и специализация Memento
- Сюжет и лор
- Локализация (только русский)
- Прогрессивное открытие разделов Справки
- Поиск по статьям
- Внешняя ссылка на memento-wiki

## 8. Тестирование

- Unit: `articles.ts` — все `id` уникальны, порядок совпадает с `HELP_ARTICLE_ORDER`, первый id = `memento`, параграфы непустые.
- Component smoke (опционально): `CampaignHelpTab` рендерит 8 панелей `Collapse`.
- Ручная проверка: вкладка доступна при активном бою; Memento Mori раскрыта по умолчанию; тексты читаются на узкой ширине.

## 9. Следующий шаг

После ревью этого файла — skill **writing-plans**: план реализации UI и `articles.ts`.
